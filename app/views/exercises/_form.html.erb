<%= simple_form_for([@training, @training.exercises.build]) do |f| %>
  <%= f.input :name%>
  

  <% @set = f.input :sets %>
  <% while @set > 1 do%>
    <%= f.input :weight%>
    <%= f.input :reps%>
    <br>
  <% end %>

  <%= f.button :submit%>
<% end %>

<%= link_to "Cancel", root_path %> | 